Your goal is to update any vulnerable dependencies.

do the following
Run `npm audit` to find vulnerable installed packages in the project.
Run `npm audit fix` to apply updates
Run test and verify the updates didnt break anything
